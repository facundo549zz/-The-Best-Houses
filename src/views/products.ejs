<!DOCTYPE html>
<html lang="es">
  <%- include('partials/head') %>
  <body>
    <%- include('partials/header') %>
    
    <h2 class="ml-3"><%= title %>:</h2>
    <div class="container-fluid">
      <div class="row">
        <div class="col-6 col-md-3 todosLosProductos">
          <section>
            <% productos.forEach(producto =>{ %>
            <div class="prd ml-2">
              <%= producto.marca %> <%= producto.nombre %> <br />
              $<%= producto.precio %>
              <a href="/products/detalle/<%= producto.id %>">
              <img src="/images/bici/<%= producto.fotos %>" alt=""/></a>
            </div>
            <% }) %>
          </section>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
