<!DOCTYPE html>
<html lang="es">

  <%- include('partials/head') %> 
  
  <script>
    function myFunction() {
      var x = document.getElementById("inputPassword3");
      if (x.type === "password") {
        x.type = "text";
      } else {
        x.type = "password";
      }
    }
  </script>
  <body>
    <%- include('partials/header') %>
    <h1 class="regis"><%= title %></h1>
    <div class="divvv">
      <img src="/images/logo.jpeg" alt="" />
      <form method="POST" action="" enctype="multipart/form-data">
        <div class="form-row">
          <div class="col-md-6 mb-3">
            <label for="Nombre">Nombre</label>
            <input
              placeholder="Nombre"
              type="text"
              class="form-control"
              id="Nombre"
              name="nombre"
              value="<%= (typeof old != 'undefined' && old.nombre)?old.nombre:'' %>" 
            />
          </div>
          <div class="col-md-6 mb-3">
            <label for="Apellido">Apellido</label>
            <input
              placeholder="Apellido"
              type="text"
              class="form-control"
              id="Apellido"
              name="apellido"
              value="<%= (typeof old != 'undefined' && old.apellido)?old.apellido:'' %>" 
            />
          </div>

          <div class="col-md-6 mb-3">
            <label for="email">Email</label>
            <input
              placeholder="email"
              type="email"
              class="form-control"
              id="email"
              required
              name="email"
              value="<%= (typeof old != 'undefined' && old.email)?old.email:'' %>"
            />
          </div>
          <div class="col-md-6 mb-3">
            <label for="password">Contraseña</label>
            <input
              placeholder="Contraseña"
              type="password"
              class="form-control"
              name="password"
              id="password"
            />
          
        </div>

        
       
          <div class="col-md-6 mb-3">
            <label for="fecha">Fecha de Nacimiento (Opcional)</label>
            <input
              placeholder="Fecha de Nacimiento"
              type="text"
              class="form-control"
              name="fecha"
              id="fechaDeNacimiento"
            />
          </div>
        
              <div class="col-md-6 mb-3">
              <div class="custom-file avatar">
              <label class="custom-file-label" for="customFileLang">Imagen de perfil</label>
              <input placeholder="Seleccionar Archivo" type="file" class="form-control" id="customFileLang" lang="es" name="image" multiple/>
              </div>
              </div>

        </div>

        

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <div class="checkbox">
              <label>
                <input type="checkbox" /> Acepto los
                <a href=""> Términos y condiciones </a> y las
                <a href=""> Políticas de privacidad </a> de The Best Bikes
                <br />
                <input type="checkbox" /> Deseo recibir
                <strong> Ofertas </strong> de esta pagina
              </label>
            </div>
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">Registrar</button>
          </div>
        </div>
      </form>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
